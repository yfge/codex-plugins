{
  "name": "feishu-codex-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Feishu Codex plugin MCP server.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
